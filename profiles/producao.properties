# producao

ambiente=producao

persistence.hibernate.show_sql=false
persistence.hibernate.format_sql=false
persistence.hibernate.hbm2ddl.auto=validate
persistence.hibernate.dialect=org.hibernate.dialect.Oracle10gDialect

profile.sistema.nome=${project.parent.artifactId}
profile.sistema.versao=${project.parent.version}

log.severity=INFO